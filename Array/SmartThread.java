class SmartThread extends SewingThread {

    SmartThread(String color, int length, String type, String brand) {
        super(color, length, type, brand);
        System.out.println("SmartThread constructor called");
    }
}